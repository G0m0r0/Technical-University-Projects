using System;
using System.Linq;

namespace reverse_string_by_recursion
{
    class Program
    {
        static void Main(string[] args)
        {
            string[] line = Console.ReadLine().Split("").ToArray();
            Recursion(line);
        }
        static void Recursion(string[] line)
        {

        }
    }
}
